# Default owner for everything in the repo.
# Must be a GitHub login (username), not a display name — htcom-code's display
# name happens to be "htjulia", but @-references resolve by login only.
# See https://docs.github.com/articles/about-code-owners
* @htcom-code
